{{ form_start(form) }}
    {{ form_row(form.team) }}    
    {{ form_row(form.user) }} 
    {{ form_row(form.result) }} 
     <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
   
